import * as Types from "@/types";

import Styles from "./auth-verification.module.scss";

export const AuthVerification = (props: Types.AuthFormProps) => {
  return <main>Auth Verification</main>;
};
